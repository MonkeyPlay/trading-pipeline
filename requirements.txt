# Core pipeline
pandas>=2.0
numpy>=1.24
pytz>=2023.3
python-dotenv>=1.0

# Database: PostgreSQL + TimescaleDB (see docker-compose.yml for a local server)
psycopg[binary]>=3.1

# Dashboard (NiceGUI + TradingView Lightweight Charts).
# The charting library itself is vendored at
# dashboard/components/lib/lightweight-charts.standalone.js, so the dashboard
# renders with no CDN and no network access.
nicegui>=3.17

# Market data collection (Interactive Brokers official API)
ibapi>=9.81

# LLM providers (optional at runtime — the forecaster falls back to a
# deterministic baseline engine if these are missing or no API key is set)
openai>=1.0
anthropic>=0.39
